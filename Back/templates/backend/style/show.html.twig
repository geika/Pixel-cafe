{% extends 'base.html.twig' %}

{% block title %}Style{% endblock %}

{% block body %}
    <h1>Style</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ style.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ style.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ style.description }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ style.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('style_index') }}">back to list</a>

    <a href="{{ path('style_edit', {'id': style.id}) }}">edit</a>

    {{ include('backend/style/_delete_form.html.twig') }}
{% endblock %}
