{% extends 'base.html.twig' %}

{% block title %}Ranking{% endblock %}

{% block body %}
    <h1>Ranking</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ranking.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ranking.name }}</td>
            </tr>
            <tr>
                <th>Place</th>
                <td>{{ ranking.place }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ ranking.score }}</td>
            </tr>
            <tr>
                <th>Reward</th>
                <td>{{ ranking.reward }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ranking_index') }}">back to list</a>

    <a href="{{ path('ranking_edit', {'id': ranking.id}) }}">edit</a>

    {{ include('backend/ranking/_delete_form.html.twig') }}
{% endblock %}
